{
  "version": 2,
  "installCommand": "cd ../.. && pnpm install",
  "buildCommand": "cd ../.. && pnpm --filter @arbinexus/api build",
  "routes": [
    { "src": "/(.*)", "dest": "/api/index.ts" }
  ]
}
