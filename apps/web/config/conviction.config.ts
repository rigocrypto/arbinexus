// Conviction market config
// Set `externalPnlUrl` to a PNL market URL after creating the external market.
export const convictionConfig = {
  externalPnlUrl: "",
  githubUrl: "",
  // Allow overriding deadline checks in demo mode (set true for demo resolves before deadline)
  demoResolverOverride: false,
};
